name: IDLMS Reuse Platform Main
on:
  workflow_dispatch:
  push:
    branches: [ main ]
jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Workflow alive ✅"
